# Live Golden-Path Evidence (real Qwen3.7 + Wan2.7 + OSS)

A complete live run of the Circle Take loop on real Qwen Cloud, 2026-06-21. Generated by
`scripts/run_golden_path_live.py all`. The `.mp4` clips are too large for git but live in OSS
(`circle-take-media/live/`); the JSON + composite here are the committed proof.

| File | What it proves |
|---|---|
| `continuity_verdict.json` | **Real Continuity Court verdict** — `qwen3.7-plus` (vision) judged the actual Wan-generated Take 1 frame and returned `verdict: fail` with structured violations (actor drift / missing markers). The differentiator, not staged. |
| `anchor_gate.json` | **Real Anchor Gate** — vision-scored the Take 2 frame; `quarantine` (scores 15/85/10) because the generated take didn't fully match Luna's fixed markers. Honest: the gate is strict and refuses non-compliant anchors. |
| `production_report.json` | Full run: real contracts → storyboard → shot-risk → verdict → reshoot spell → anchor gate → memory. |
| `before_after.png` | Take 1 (CUT) vs Take Two, from the real generated frames. |

Pipeline proven end-to-end: **Qwen contracts → Wan 2.7 video (create→poll→mp4) → ffmpeg frame
→ Qwen-vision verdict/gate → OSS upload.** Demo-failure strategy = Option B (transparent
constructed): Take 1/Take 2 are two real generations; all verdicts come from real vision calls.
