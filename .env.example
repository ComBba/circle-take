# --- Qwen Cloud (DashScope, intl) ---
QWEN_API_KEY=replace_me  # optional — only for offline scripts/; the web app is BYOK (X-Qwen-Key)
QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_VIDEO_BASE_URL=https://dashscope-intl.aliyuncs.com/api/v1

# --- Model IDs (centralized; lock from docs/verified_models.md after Task 0.2) ---
QWEN_TEXT_MODEL=qwen3.7-plus
QWEN_VISION_MODEL=qwen3.7-plus
WAN_T2V_MODEL=wan2.7-t2v
WAN_I2V_MODEL=wan2.7-i2v
WAN_R2V_MODEL=wan2.7-r2v
WAN_VIDEOEDIT_MODEL=wan2.7-videoedit

# --- Alibaba Cloud (OSS storage = deployment proof) ---
ALIBABA_CLOUD_REGION=replace_me
ALIBABA_CLOUD_OSS_BUCKET=replace_me
ALIBABA_CLOUD_ACCESS_KEY_ID=replace_me
ALIBABA_CLOUD_ACCESS_KEY_SECRET=replace_me

# --- App ---
DATABASE_URL=sqlite:///./circle_take.db  # ephemeral episode state; no durable storage
APP_ENV=development  # development | fixture | live
# Live web runs are BYOK: the browser sends the caller's key via the X-Qwen-Key header,
# used per request and never stored or logged. No accounts, no JWT.

# --- Judge-live path (optional) — let judges run live WITHOUT their own key ---
# Set BOTH to enable. JUDGE_QWEN_KEY = the owner's DashScope key (spends owner free-tier);
# JUDGE_CODE = the passcode you publish to judges (Devpost testing instructions) so only
# they, not the public, can trigger it (judges visit /ui/?code=<JUDGE_CODE>). Capped at
# JUDGE_DAILY_CAP live runs/day. Both empty -> judge-live path is OFF (BYOK + fixtures only).
JUDGE_QWEN_KEY=   # owner's Qwen key for judge-funded live runs (empty to disable)
JUDGE_CODE=       # passcode judges enter (?code=... or X-Judge-Code header)
JUDGE_DAILY_CAP=10
